name: CI/CD
on: push


jobs:
  run-my-command:
    runs-on: ubuntu-latest
    steps:
      - name: Copy Files
      - uses: actions/checkout@v4 # копируем репозиторий чекаутом
        run:
          ls -a




