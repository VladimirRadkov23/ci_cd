name: Run Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_call:
  workflow_dispatch:
  steps:
    - name: Display some Asd
      run: echo "Some text"